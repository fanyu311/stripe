<section class="container mt-4">
	<h1 class="text-center">Mes Commandes</h1>
	<table class="table table-striped table-light mt-4">
		<thead>
			<tr>
				<th>Id</th>
				<th>Nom de produit</th>
				<th>Prix</th>
			</tr>
		</thead>
		<tbody>
			{% for product in products %}
				<tr>
					<td>{{ product.id }}</td>
					<td>{{ product.name }}</td>
					<td>{{ product.price }}€</td>
					<td>
						<div class="d-flex justify-content-between">
							<a href="{{ path('app.orders.index') }}" class="btn btn-warning">Payer</a>
							{% include 'Backend/Product/_deleteForm.html.twig' %}
						</div>
					</td>
				</tr>
			{% endfor %}
		</tbody>
	</table>
</section>
